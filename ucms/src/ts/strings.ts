const protocol: string = 'http'
const domain: string = 'localhost'
const port: string = '8080'
const context: string = 'ucms'

const url: string = protocol + '://' + domain + ':' + port + '/' + context

export {
    url
};